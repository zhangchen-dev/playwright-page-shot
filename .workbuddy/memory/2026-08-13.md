# 2026-08-13

- 完成「录制页面字段 ↔ 地图元素」对应关系图：基于 `src/map-preview.js` 的 `transformRecordingToMockConfig` 与 `src/map-template/index.html`，梳理出 14 条字段→地图元素映射，按「配置阶段 / 录制阶段·模块与主步骤 / 元素标记」三组呈现。
- 产出：Ardot 画布（fileId 714321674738554）+ PNG 导出 `output/录制字段-地图元素对应图.png`，供用户后续加入 banner。

- 环境/CSS 地址处理改造（采用方案2：保留保存时选环境的交互，CSS 与导航脚本统一按环境切换地址）：
  - `renderer/modules/common/feedback.js` 的 `showEnvConfigDialog` 环境选项由仅 dev/prd 改为 local/dev/prd 三选一；local 的 envBaseUrl 为空，预览文案区分相对/绝对地址。
  - `src/recorder.js` 新增 `_applyAbsoluteResourceUrls(originName)`：远端环境（dev/prd）把 HTML 内 `./stepX.css`、iframe `src` 及 iframe 内 CSS 改为 `originName + 文件名` 全域名绝对地址；local 保持相对。在 `_endAndSave` 中于 `_injectOriginName` 之后调用。删除旧的无用方法 `_applyResourceBaseUrl`。
  - `main/ipc/recording-mgmt-ipc.js` 的 `sync-to-prd` 在改写 HTML 时同步把旧 origin 替换为新 origin（覆盖 CSS href/iframe src）。
  - 约定：local=相对地址（双击即用），dev/prd=带域名绝对地址，与 HTML 自身地址一致；导航脚本 `originName` 同步注入。
