/**
 * JSON 配置转换器
 * 后续在此处添加具体的转换逻辑（如字段映射、格式调整等）
 * 目前为占位实现，原样返回
 */

/**
 * 转换 demo_config.json 配置
 * @param {object} config - 原始配置对象
 * @returns {object} 转换后的配置对象
 */
function transformConfig(config) {
  // TODO: 后续实现具体的转换逻辑
  // 例如：字段重命名、格式调整、数据补全等
  return config;
}

module.exports = { transformConfig };
